# Minimal Supabase CLI config — `supabase start` fills in defaults.
project_id = "specimap"

[auth]
site_url = "http://localhost:5173"
additional_redirect_urls = ["http://localhost:5173/auth/callback"]

[auth.email]
enable_signup = true
enable_confirmations = false
